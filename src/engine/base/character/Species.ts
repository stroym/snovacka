export default class Species {

  // name: string;

  //should contain some lore things about a species, probably some subspecies and things, descriptions etc...

  //TODO use concrete impementations as is the case with characters
}
